[pytest]
testpaths = test
pythonpath = worker/src
addopts = -v -s --log-cli-level=INFO --log-cli-format="%(asctime)s [%(levelname)s] %(message)s"
markers =
    integration: tests that hit a live Supabase/API instance
